# AmberClaw Master Feature Protocol

> **Purpose**: Use this prompt when requesting AmberClaw to implement any new feature, module, or integration. It ensures the AI adheres to the project's high-performance, unified architecture.

---

### 🚀 THE COMMAND
**"Reconstruct and unify [FEATURE_NAME] into the AmberClaw core using the Production-Grade Feature Protocol."**

### 🧠 INSTRUCTIONS FOR THE AI
1. **Research First**: Deep-dive into all sources/legacy code to identify the core IP of [FEATURE_NAME]. Never assume; verify architectural compatibility.
2. **Unified Reconstruction**:
    - Add the feature to `amberclaw/features/` (logic) or `amberclaw/engine/` (performance).
    - Maintain **Pure Python** standards for cross-platform support (Android/Linux/Windows).
    - Ensure zero redundancy — delete unused or duplicated legacy folders immediately after migration.

3. **Antigravity Execution**:
    - Invoke relevant **Antigravity Skills** (e.g., `@python-pro`, `@senior-fullstack`, `@code-reviewer`) before every action.
    - Adhere to **@caveman** token optimization for all internal thought processing.
4. **Production Excellence**:
    - Stick to the unified CLI-first interface (Rich-based UI).
    - Ensure all code is production-ready, type-checked, and self-documenting.
    - Update `README.md` to reflect the new capabilities.

---

### 🎯 ACCEPTANCE CRITERIA
- No legacy `feature/` folders remain.
- Feature works natively in Termux (Android), Ubuntu (Linux), and Windows CMD.
- Logic is non-destructive and preserves existing `amberclaw` functionality.
- The UX is "mind-blowing" — professional, fast, and intelligent.
